syntax = "proto3";

package rat;

service ServiceTester {
    rpc Send (RequestTester) returns (ResponseTester);
}

message RequestTester {
    string words = 1;
    string thoughts = 2;
}

message ResponseTester {
    string words = 1;
    string thoughts = 2;
}