[workspace]

members = [
    "hash_params",
    "process_hollowing",
    "process_migration",
    "rco_config",
    "rco_utils",
    "tcp_reverse_shell",    
    "xor_params",
]

resolver = "2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = true
